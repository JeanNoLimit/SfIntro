{% extends 'base.html.twig' %}

{% block title %}Ajouter une entreprise{% endblock %}

{% block body %}

    {% if edit %}
        <h1>Modifier l'entreprise {{  entreprise.raisonSociale }}</h1>
    {% else %}
        <h1>Ajouter une entreprise</h1>
    {% endif %}

    {# form est une fonction de twig. formAddEntreprise est la variable dans le return de la fonction add du controller #}
    {{ form(formAddEntreprise) }}


{% endblock %}
