{% extends 'base.html.twig' %}

{% block title %}Hello EntrepriseController!{% endblock %}

{% block body %}
    <h1>Liste des entreprises</h1>

    <a class="btn btn-primary" href="{{path('add_entreprise')}}">Ajouter une entreprise</a> <br>

{# {{}} Sert à faire un echo dans un twig! #}
   
    {% for entreprise in entreprises %}
        {# ~ permet de faire une concaténation dans un twig. le point . permet d'appeler une varible d'un objet. (remplace getRaisonSociale()) #}
        {# {{ entreprise.raisonSociale ~ ' (' ~ entreprise.ville ~ ')' }} <br> #}
        {# Ou avec méthode __toString() : #}
        <a href="{{ path('show_entreprise', {id: entreprise.id})}}">{{entreprise}}</a> <br>

    {% endfor %}



{% endblock %}
